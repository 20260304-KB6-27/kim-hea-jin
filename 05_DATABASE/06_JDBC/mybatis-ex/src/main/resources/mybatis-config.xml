<?xml version="1.0" encoding="UTF-8" ?>

<!--
XML(Extensible Markup Language)
- 데이터를 구조화해서 저장하고 전달하기 위한 마크업 언어
- 작성 규칙: "http://mybatis.org/dtd/mybatis-3-config.dtd"
-->

<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">

<!--
태그 작성 순서 (순서 다르면 에러)
: properties -> settings -> typealiases -> enviroments -> mappers
-->

<configuration>

    <!--
    <properties>: 외부 설정파일 읽어옴
    -->
    <properties resource="application.properties" />

    <settings>
        <!-- 2차 캐시 사용 여부 -->
        <setting name="cacheEnabled" value="false" />
        <!-- 컬럼명(DB)을 필드명(Java)에 맞춰 자동 변환 -->
        <setting name="mapUnderscoreToCamelCase" value="true" />
        <!-- insert 시 생성되는 (auto-increament) PK값을 객체에 채울지 여부 -->
        <setting name="useGeneratedKeys" value="false" />
    </settings>

    <!-- 중요: 전체 패키지 경로(사용자 정의 클래스) 대신 별칭 사용 -->
    <typeAliases>
        <typeAlias type="org.scoula.travel.domain.TravelVO" alias="TravelVO"/>
    </typeAliases>

    <!-- DB 연결 환경설정 -->
    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC" />

            <!-- 커넥션 풀 사용 (application.properties 읽어와 매핑) -->
            <dataSource type="POOLED">
                <property name="driver"   value="${driver}" />
                <property name="url"      value="${url}" />
                <property name="username" value="${username}" />
                <property name="password" value="${password}" />
            </dataSource>
        </environment>
    </environments>

    <!-- 중요: SQL Mapper XML 파일 등록 -->
    <mappers>
        <!-- 아래 파일에서 SQL문 가져옴 -->
        <mapper resource="mapper/travel-mapper.xml"/>
        <!-- 새로운 Mapper 파일 생성 시 여기에 등록 -->
    </mappers>
</configuration>